config {
  type: "view",
  schema: "dataform_ecommerce_staging",
  tags: ["staging"],
  description: "注文ヘッダ"
}

SELECT
  order_id,
  user_id,
  status,
  created_at
FROM ${ref("bigquery-public-data", "thelook_ecommerce", "orders")}
WHERE
  DATE(created_at) >= '2025-01-01'
