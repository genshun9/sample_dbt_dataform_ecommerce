config {
  type: "view",
  schema: "dataform_ecommerce_staging",
  tags: ["staging"],
  description: "注文明細"
}

SELECT
  order_id,
  inventory_item_id,
  sale_price,
  created_at
FROM ${ref("bigquery-public-data", "thelook_ecommerce", "order_items")}
WHERE
  DATE(created_at) >= '2025-01-01'
